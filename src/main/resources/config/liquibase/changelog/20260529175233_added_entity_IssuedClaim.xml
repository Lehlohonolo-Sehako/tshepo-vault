<?xml version="1.0" encoding="utf-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd
                        http://www.liquibase.org/xml/ns/dbchangelog-ext http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-ext.xsd">

    <!--
        Added the entity IssuedClaim.
    -->
    <changeSet id="20260529175233-1" author="jhipster">
        <createTable tableName="issued_claim" remarks="One predicate fact certified inside a Credential.\nPRIVACY: `met` is the only boolean stored. The raw evidence\n(e.g. &quot;R42,300&quot;) is computed in memory, shown to the holder\nduring issuance, and immediately discarded — it never persists here.">
            <column name="id" type="bigint">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="claim_type" type="varchar(255)">
                <constraints nullable="false" />
            </column>
            <column name="operator" type="varchar(255)">
                <constraints nullable="false" />
            </column>
            <column name="threshold" type="decimal(21,2)" remarks="Threshold value (Rand for monetary types, months for TENURE).">
                <constraints nullable="false" />
            </column>
            <column name="currency" type="varchar(3)">
                <constraints nullable="true" />
            </column>
            <column name="period_months" type="integer" remarks="Look-back window used when computing this claim.">
                <constraints nullable="true" />
            </column>
            <column name="met" type="boolean" remarks="True if the holder satisfied this threshold at issuance time.">
                <constraints nullable="false" />
            </column>
            <column name="credential_id" type="bigint">
                <constraints nullable="false" />
            </column>
            <!-- jhipster-needle-liquibase-add-column - JHipster will add columns here -->
        </createTable>
    </changeSet>

    <!-- jhipster-needle-liquibase-add-changeset - JHipster will add changesets here -->

    <!--
        Load sample data generated with Faker.js
        - This data can be easily edited using a CSV editor (or even MS Excel) and
          is located in the 'src/main/resources/config/liquibase/fake-data' directory
        - By default this data is applied when running with the JHipster 'dev' profile.
          This can be customized by adding or removing 'faker' in the 'spring.liquibase.contexts'
          Spring Boot configuration key.
    -->
    <changeSet id="20260529175233-1-data" author="jhipster" context="faker">
        <loadData
                  file="config/liquibase/fake-data/issued_claim.csv"
                  separator=";"
                  tableName="issued_claim"
                  usePreparedStatements="true">
            <column name="id" type="numeric"/>
            <column name="claim_type" type="string"/>
            <column name="operator" type="string"/>
            <column name="threshold" type="numeric"/>
            <column name="currency" type="string"/>
            <column name="period_months" type="numeric"/>
            <column name="met" type="boolean"/>
            <column name="credential_id" type="numeric"/>
            <!-- jhipster-needle-liquibase-add-loadcolumn - JHipster (and/or extensions) can add load columns here -->
        </loadData>
    </changeSet>
</databaseChangeLog>
