<?xml version="1.0" encoding="utf-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd
                        http://www.liquibase.org/xml/ns/dbchangelog-ext http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-ext.xsd">

    <!--
        Added the entity VerificationEvent.
    -->
    <changeSet id="20260529175235-1" author="jhipster">
        <createTable tableName="verification_event" remarks="Append-only audit log of every verification attempt.\napiKey is null when the check came from the public browser page.\ndisclosedClaims records which ClaimType values the holder revealed —\nstored as a JSON array string for portability.">
            <column name="id" type="bigint">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="verified_at" type="${datetimeType}">
                <constraints nullable="false" />
            </column>
            <column name="result" type="varchar(255)">
                <constraints nullable="false" />
            </column>
            <column name="disclosed_claims" type="${clobType}" remarks="JSON array of ClaimType strings that were disclosed.">
                <constraints nullable="true" />
            </column>
            <column name="credential_ref" type="varchar(200)" remarks="URN reference to the credential that was presented (for audit).">
                <constraints nullable="true" />
            </column>
            <column name="api_key_id" type="bigint">
                <constraints nullable="true" />
            </column>
            <!-- jhipster-needle-liquibase-add-column - JHipster will add columns here -->
        </createTable>
        <dropDefaultValue tableName="verification_event" columnName="verified_at" columnDataType="${datetimeType}"/>
    </changeSet>

    <!-- jhipster-needle-liquibase-add-changeset - JHipster will add changesets here -->

    <!--
        Load sample data generated with Faker.js
        - This data can be easily edited using a CSV editor (or even MS Excel) and
          is located in the 'src/main/resources/config/liquibase/fake-data' directory
        - By default this data is applied when running with the JHipster 'dev' profile.
          This can be customized by adding or removing 'faker' in the 'spring.liquibase.contexts'
          Spring Boot configuration key.
    -->
    <changeSet id="20260529175235-1-data" author="jhipster" context="faker">
        <loadData
                  file="config/liquibase/fake-data/verification_event.csv"
                  separator=";"
                  tableName="verification_event"
                  usePreparedStatements="true">
            <column name="id" type="numeric"/>
            <column name="verified_at" type="date"/>
            <column name="result" type="string"/>
            <column name="disclosed_claims" type="clob"/>
            <column name="credential_ref" type="string"/>
            <!-- jhipster-needle-liquibase-add-loadcolumn - JHipster (and/or extensions) can add load columns here -->
        </loadData>
    </changeSet>
</databaseChangeLog>
